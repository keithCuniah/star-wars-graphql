import { createContext } from 'react';

// FIXME: the context is not update properly when the user visite the detailPage 
// context to update the list of last 3 detail pages visited
export const DetailPagesContext = createContext({});
